# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: 285 Study Guide
description: A study guide for ECE285 Circuit Analysis
baseurl: "/circuit-analysis-study-guide" # the subpath of your site, e.g. /blog
url: "https://teekkar.github.io" # the base hostname & protocol for your site, e.g. http://example.com


# Build settings
theme: "just-the-docs"

# Color scheme currently only supports "dark", "light"/nil (default), or a custom scheme that you define
color_scheme: dark

# Config settings:

# Enable or disable the site search
# Supports true (default) or false
search_enabled: false

# Exclude from processing.
exclude:
   - .sass-cache/
   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
